fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-dotenv>=1.0

# Bedrock. only needed when LLM_PROVIDER=bedrock; the mock adapter needs nothing.
boto3>=1.35

# tests
pytest>=8.3
httpx>=0.27
